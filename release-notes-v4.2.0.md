# DeepSeek Harness Desktop v4.2.0

## 新建会话与 Computer Use 挂载

- 上游 0.1.6 起预置了 Computer Use 能力，其中负责独占服务的行会发布进程级全局服务；预设挂载这类行时会因作用域不合法而整体失败，用户侧表现为“打不开新对话”或新会话反复失败。
- 桌面版把这些行放进带 `isolate` 隔离域的 `cordis:group` 中，让独占服务在独立作用域内注册，新建会话与会话回复恢复正常。
- Cua Driver 原生提供方与 Computer Use 服务同组随安装包默认启用，无需用户手动安装插件。

## 内核升级到 dsh-v0.1.6-alpha.2

- Harness runtime 跟随官方最新预发布标签 `dsh-v0.1.6-alpha.2`，精确来源见 `upstream-lock.json`。
- 0.1.6 把 profile 组合移入受保护分支、扩宽 compose 签名并调整启动环境注入顺序；运行时补丁按新结构重新锚定，保留启动诊断、compile cache 刷写和预设增强。
- 插件预设迁移到 `@deepseek-ai/dsh-agent-presets`，桌面版据此更新预设打补丁的目标路径。
- Computer Use 的两个包没有任何上游模块依赖它们，因此作为显式闭包根加入运行时清单，确保随安装包交付。

## 启动与稳定性

- 运行时不兼容补丁沿用“单点失败不阻断启动”的策略：某个上游锚点漂移时只跳过该补丁并记录诊断，不会连带阻断整个桌面启动。

v4.0.4 的会话竞态、设置退出入口和窗口控件动画修复一并保留；多 IM 接入、内置插件商店、窗口状态记忆与经过 CI 验证的人工发布流程延续 v4.0.3 及更早版本。
