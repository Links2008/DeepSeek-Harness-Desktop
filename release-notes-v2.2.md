# DeepSeek Harness Desktop v2.2

这次更新聚焦设置可靠性、一级菜单整理、插件商店入口与云母视觉修复，并把运行时兼容补丁纳入安装包。

## 主题切换修复（设置持久化）

- 修复深色/浅色/跟随系统主题切换失效的问题。
- 设置持久化层（dsh-atomic-write）使用 `<file>.lock` 串行化跨进程写入；后端崩溃会残留 `settings.yaml.lock`，导致之后所有设置写入超时回滚。
- 桌面壳启动时检测锁内 PID，若进程已死亡则自动清理孤儿锁（`healOrphanedSettingsLock`），恢复设置可写入。
- 对主题运行时打补丁：写入采用序列号排序，避免过期偏好覆盖最新选择，切换后立即生效并持久化。

## 一级菜单整理

- 移除一级菜单的任务看板：插件入口与看板视图一并隐藏，不再占用侧栏。

## 插件商店入口（一级菜单）

- 在侧栏一级菜单新增「插件商店」按钮，点击直达设置中的 DSH 插件市场页面。
- 入口随 SPA 重渲染自动补回，不会因页面刷新丢失。
- 插件市场已更新到 v1.5.1，并清理了外部插件残留依赖，避免重复写回 `package.json`。

## 云母效果修复

- 修复切换到云母（Mica）效果时，会话框底部命中数据栏过宽导致 `margin:0 auto` 失效、向右偏移的问题。
- 改用 `left:50% + translateX(-50%)` 强制水平居中，窄内容时与原布局等价。

## 运行时兼容补丁（安装包内置）

- 安装包新增 `scripts/patch-harness-runtime.cjs`，启动时对运行时应用兼容补丁：
  - 主题写入顺序与过期偏好防护；
  - 上下文压缩阈值与安全余量调整；
  - 会话图片拖放桥接；
  - minimal 预设内置上下文压缩；
  - 客户端插件（如云母主题）自动激活。

## 发布流程

- 发布策略支持作者手动 bump 版本（`authorBumped`），避免 CI 对已声明版本二次 bump，保证发布 tag 与版本一致。
- 本次发布 tag 为 `v2.2`，对应版本 `2.2.0`。
