# DeepSeek Harness Desktop v4.0.2

本版针对 Issue #9 的启动恢复、窗口状态和诊断问题，并修复发布流水线的原生模块 ABI 失败。

## 变化

- 内核升级到官方发布标签 `dsh-v0.1.3-alpha.1`，确切来源见 `upstream-lock.json`。这是上游预发布版本。
- 保存窗口正常边界与最大化状态；窗口回到可见显示区域，损坏状态文件回退默认值。
- 插件恢复识别无 `/dsh` 后缀和嵌套依赖导入失败，保留 profile 清单备份，限定第三方插件隔离，显示具体原因与诊断入口。
- 桌面和后端统一遵循 `DSH_HOME`，避免自定义数据目录下的插件准备误用默认 profile。
- 新 profile 默认启用内置 `dshmarket` 商店，已有 profile、个人插件和凭据不覆盖。社区插件安装和商店目录访问需要联网。
- README 补充 DSH-IM 支持的九个渠道及配置要求，加入邮件对应的 dsh.so 动态收录/检测徽章，不将其称作安全认证。

## 发布与更新

- `fs-ext` 使用 `@electron/rebuild` 为当前 Electron 重编译，随后以真实 Electron 加载验证；一并检查终端、图像和 FFI 原生能力。
- 每日监控选择官方已发布 DSH 标签，不再跟随未发布的 master HEAD；发布仍由人工账号完成。
- 仅成功验收后上传安装器、blockmap、更新清单和版本锁；人工发布可下载同一 HEAD 的准确 CI 制品并核对锁文件。
- 安装/卸载验收拒绝在已有正式安装的机器执行，避免验收清理移除用户安装和快捷方式。

## 边界

- 设置页使用当前上游 Typert 接口；旧 `/api/settings.describe` 路径不再是健康检查依据。
- 多 IM 是经 DSH-IM 配置后接入，不代表所有渠道已在本机验证连接。
- 无人值守多会话编排、轮边界排水重启仍为后续功能提案。
- 本版未商业签名；请以 GitHub Release 附件和 SHA-256 核验来源。
