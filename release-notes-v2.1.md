# DeepSeek Harness Desktop v2.1

这次更新集中修复 Windows 桌面端的启动、窗口交互、通知、更新和本机 Web profile 兼容问题。

## 窗口与三色控件（桌面壳）

- 窗口立即显示，不再播放启动页或启动动画。
- 三色控件整体下移 3px，并记住上次侧栏状态，冷启动与正式界面保持同一位置。
- 侧栏只上报展开/收起状态，桌面主进程用 160ms 可中断位移同步控件，避免逐帧跨进程 IPC 卡顿。
- 保持系统直角窗口，不添加自定义窗口圆角、裁切、光晕或弹跳。

## 更新入口（侧栏）

- 复用原有检查更新位置，改为 40x40px 深色圆形底和白色下载 SVG，不显示文字。
- hover 仅轻微提亮，按下仅轻缩；无旋转、弹跳和光晕。
- 修复 SPA 重渲染后更新监听器累积的问题。

## 自动更新与发布（桌面壳 + GitHub Release）

- 仅在用户点击下载图标时检查更新，不做后台定时检查。
- 下载完成后通过同一按钮重启安装完整 Setup EXE。
- Release 只包含 `DeepSeekHarness-Setup-2.1.0.exe` 与 electron-updater 必需的 `latest.yml`，不发布 ZIP 或 app.asar 补丁。

## 通知与任务跳转（桌面壳）

- 任务完成通知记录目标会话 URL；点击通知会恢复、置前、聚焦并跳回对应任务。
- 任务节点从侧栏消失时补发完成事件，降低漏通知概率。

## Web profile（用户配置目录）

- 主题聚合包统一到 0.1.17，Vision Router 升级到 1.3.0。
- 会话总结插件的市场 npm 地址未公开发布，v2.1 改用其官方 GitHub 源修复本机 profile；结构化时间线无需 API Key，AI 总结仍需用户配置有效的兼容接口。
