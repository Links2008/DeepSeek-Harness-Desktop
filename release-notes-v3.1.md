# DeepSeek Harness Desktop v3.1

本次聚焦三类修复：冷启动卡顿根因、侧栏交互失灵、界面细节统一。

## 冷启动卡顿（根因修复）

- 修复启动等待可达 40 秒以上的问题。根因：壳层对 `node_modules` 的文件监视在启动期被 pnpm 自检触发，形成"事件风暴 → 页面反复重载（ERR_ABORTED）→ 与后端引导互相争抢"的恶性循环。
- 监视器重写：只对 `package.json` 与 `cordis.patch.yml` 两个清单做低频内容摘要比对（SHA-1），不再监视 `node_modules` 目录。
- 三重防护：后端就绪前不触发重载；变更静默 5 秒才合并执行；两次重载至少间隔 15 秒。
- 监视器挂载时机移到后端就绪之后，启动期 pnpm 自检完全不再干扰加载。

## 交互失灵修复（与冷启动同根因）

- 修复 dsh-better-sidebar 文件栏无法正常打开：页面被重载循环打断，点击被吞。
- 修复设置对话框"取消"按钮无法点击：同上，点击落在重载间隙之外即失效。

## 界面统一

- 侧栏底部"检查更新"与"移动端远程控制"两枚按钮统一规格：同为 36px 圆形幽灵按钮、同一颜色 token、同一悬停/按下反馈，并排对齐（此前一个 40px 白色、一个 36px 灰色，大小颜色均不一致）。
- 侧栏收起时"插件商店"条目直接以纯图标插入，不再先闪现中文字样（此前最长需等 2.5 秒轮询才切换为图标）。

## 插件兼容

- 修复 `@deepseek-ai/dsh-client-ui-aqua` 加载失败（`keyed slot "settings.plugin.item" requires options.key`）：upstream slots 0.1.0-rc.7 将该插槽改为 keyed 注册，marketplace 安装的 aqua 1.3.0 仍按旧 API 注册；壳层补丁自动将其转换为 keyed 形态，aqua 官方更新后补丁自动跳过。

## 精简

- 移除已禁用的 aionui 面板（`@linxin666/dsh-client-ui-aionui-panel`）的图片拖拽补丁死代码，启动时少一次无意义的文件扫描与改写。

## 升级说明

- v3.0.0 用户：点击侧栏底部"检查更新"按钮，新版本自动下载并通知，点"重启更新"即可；直接运行本 Release 的 `DeepSeekHarness-Setup-3.1.0.exe` 覆盖安装亦可，安装器会自动关闭正在运行的应用与内置后端。