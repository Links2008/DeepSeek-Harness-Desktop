# DeepSeek Harness v3.1.1

## 修复

- **插件/商店更新后启动崩溃（本次核心修复）**：`dsh-web-ui-all` 0.2.0 起在聚合补丁中内置了 `dsh-better-sidebar` 加载入口，与 profile 中显式声明的同名插件形成双重加载——两个实例都注册 `/sidebar/api` 路由，后端启动即抛出 `duplicate prefix route` 并退出（界面表现为白屏/ERR_FAILED，即"更新完插件软件就崩溃"）。
  - 桌面壳现在会在启动与插件变更重载前自动执行**聚合入口去重**：聚合包 insert 的条目若与 profile 显式 bundle 重复，自动禁用聚合副本（保留显式加载，侧边栏功能不变）。
  - 去重逻辑幂等（重复执行不产生重复条目），单点失败仅记录日志、不影响其他补丁。

## 升级说明

- v3.1.0 用户：侧栏底部"检查更新"→ 自动下载 → 重启即完成升级（或直接运行 `DeepSeekHarness-Setup-3.1.1.exe` 覆盖安装）。
- 升级后首次启动会自动清理由插件更新引入的重复加载项，无需手动处理。
