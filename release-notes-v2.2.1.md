# DeepSeek Harness Desktop v2.2.1

这次更新修复覆盖安装/升级时"无法关闭"和"卸载旧文件失败（退出码 2）"的问题。

## 覆盖安装修复（安装器）

- 修复从 v2.2.0 及更早版本升级时，安装器弹出"DeepSeekHarness 无法关闭，请手动关闭它"的问题。
- 修复旧卸载器报 "Failed to uninstall old application files. Please try running the installer again.: 2" 的问题。
- 根因：内置 node 后端进程（含父进程已退出后残留的孤儿进程）会锁住安装目录下 `resources` 里的运行时文件，旧版静默卸载器无法自行结束占用进程，删除文件失败并返回退出码 2。
- 新安装器在调用旧版卸载器之前，先强制结束 `DeepSeekHarness.exe` 进程树，并按"可执行文件路径位于安装目录内"精确结束内置 node 后端，不会误伤用户其它 node 进程。

## 本版本起的卸载/升级行为

- 本版本及以后的卸载器在检查"应用是否在运行"时，会直接自动关闭应用与其内置后端，不再弹窗等待用户手动关闭。

## 升级说明

- 从 v2.2.0 升级到 v2.2.1 时，安装器会自动关闭正在运行的应用，无需手动结束进程。
- 若仍遇到"无法关闭"提示（例如旧版安装器卡住），可在任务管理器中结束 `DeepSeekHarness.exe` 与安装目录下的 `node.exe` 后点击"重试"。
