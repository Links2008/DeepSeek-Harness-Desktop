# DeepSeek Harness Desktop v4.0.3

## 启动黑屏修复

- 移除桌面壳隐藏上游整个启动区域的旧 CSS；该区域也负责显示前端插件加载错误，不能当作装饰性加载动画隐藏。
- 新增实际桌面样式注入回归，防止插件失败提示再次变成空白窗口。
- 后端 ready / HTTP 200 不等于主页可用；前端插件须完成加载并显示可交互界面。

## 本地插件升级提示

官方内核仍固定为已发布的 `dsh-v0.1.3-alpha.1`。旧社区插件若引用 `@deepseek-ai/dsh-client-runtime/client`，需要由插件维护者迁移至新版 SDK：状态存储位于 `@deepseek-ai/dsh-client-store`，设置和会话接口也已变化。桌面安装器不会静默禁用插件，也不会包含用户本地插件、主题改动或个人配置。

多 IM 接入、内置插件商店、窗口状态记忆与经过 CI 验证的人工发布流程延续 v4.0.2。
