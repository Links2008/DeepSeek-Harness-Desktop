{
  "name": "dsh-desktop",
  "version": "1.0.0",
  "private": true,
  "description": "Windows desktop shell and one-click installer for DeepSeek Harness",
  "main": "main.js",
  "scripts": {
    "test": "node tests/installer_runtime.test.js",
    "build:installer": "electron-builder --config electron-builder.yml --win nsis --x64"
  },
  "devDependencies": {
    "electron": "^43.4.0",
    "electron-builder": "^26.15.3"
  }
}
