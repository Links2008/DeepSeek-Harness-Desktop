# DeepSeek Harness Desktop v4.0.4

## 新建会话与插件重载竞态

- 插件清单变更会触发界面重载，而重载会丢弃正在进行的交互并吞掉点击，用户侧表现为“新建会话反复失败”或点击无响应。
- 渲染层现在上报用户交互心跳（pointerdown/keydown/wheel/input/focusin），重载在用户空闲 8 秒后才提交；期间任何新交互都会继续顺延，重载不再与操作抢跑。
- 主页就绪判定放宽为“编辑器可见即就绪”，避免就绪检测与重载窗口相互等待。

## 设置面板退出入口

- v3.1.2 移除旧的退出绑定后，设置面板里的原生“退出”按钮失去 IPC 通路（close 被拦截并 hide），点击毫无反应。
- 重新绑定文案精确匹配“退出”的原生按钮，并在底部操作栏补充桌面壳自有的退出按钮，二者统一走 win:quit，标记退出语义后 app.quit()。
- 选择器精确到文案且不使用 stopImmediatePropagation，避免再次吞掉其它按钮的正常点击；“退出登录/注销”不在此列。

## 窗口控件动画卡顿

- 窗口隐藏或最小化时，逐帧 setBounds 仍在离屏播放，恢复可见时叠加残余帧造成抖动。
- 窗口不可见/最小化时直接落到目标位置并终止动画，位置以时间为基准推进，避免回跳。

多 IM 接入、内置插件商店、窗口状态记忆与经过 CI 验证的人工发布流程延续 v4.0.3；v4.0.3 的启动黑屏错误展示修复一并保留。
